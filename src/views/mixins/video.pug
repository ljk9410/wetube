mixin video(video)
    div 
        h4
            a(href=`/videos/${video.id}`)=video.title
        p=video.description
        each hashtag in video.hashtags
            li=hashtag 
        small view: #{video.meta.views} | rating: #{video.meta.rating}
        br
        small=video.createdAt
        hr